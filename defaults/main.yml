---
# -------------------------------------------------
# Default Variables
# -------------------------------------------------
lb1152_subdomain_base: "{{ subdomain_base }}"
lb1152_satellite_node_config_internal: "{{ subdomain_internal }}"
lb1152_satellite_node_config_user_name: admin
lb1152_satellite_node_config_user_password: <CHANGEME>
lb1152_satellite_node_config_satellite_init_hosts: "{{ groups['nodes'] }}"
lb1152_satellite_node_config_hosts_satellite_bastion: "{{ groups['satellites'] + groups['bastions'] }}"
lb1152_satellite_node_config_hosts_satellites: "{{ groups['satellites'] }}"
